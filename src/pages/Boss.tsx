import StubScreen from '@/components/game/StubScreen'

/** Eigen Keep (Boss) — boss.md. Turret fight: only eigen-direction shots fly straight. */
export default function Boss() {
  return (
    <StubScreen
      title="EIGEN, the Unmoved"
      art="/boss-eigen.png"
      blurb="A turret, a warped arena, and shots that only fly true on the Unmoved Path."
    />
  )
}
