import StubScreen from '@/components/game/StubScreen'

/** Settings — settings.md. Audio, haptics, grid intensity, reduce motion, colorblind palettes, math-label toggle, reset. */
export default function Settings() {
  return (
    <StubScreen
      title="Settings"
      art="/zone-rewind-rift.png"
      blurb="Audio, haptics, grid intensity, palettes and the big red reset button."
    />
  )
}
